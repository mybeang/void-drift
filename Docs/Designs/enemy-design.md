# Void Drift — 적 설계 (유형 / AI / 에셋 관리)

> onepage-design.md TODO #2 "적 유형 최종 목록/패턴 확정".
> 수치(체력·속도 등)는 Day5 튜닝으로 이관 → 에디터 Table Tool로 관리.

## 1. 설계 원칙 — "고정 적"이 아니라 "조합형 적"

적을 3종으로 고정하지 않는다. **비주얼 아키타입 × 이동 AI × 공격 AI × 스탯**을
조합해 스폰하며, 그 조합 풀을 **Unity Editor 커스텀 툴에서 오서링**한다.

- 다양성: 3에셋 아키타입 × 4이동 × 4공격 → 조합 수십 종
- 에디터 툴이 단순 데이터 테이블을 넘어 **"조합 오서링 + 검증 툴"**로 격상
  (공고 1순위 "에디터 커스텀 툴" 정면 대응, CMDB 검증 경력과 서사 연결)

## 2. 적 엔티티 모델

적 인스턴스 = 아래 4요소의 조합.

| 구성요소 | 내용 |
|---|---|
| 비주얼(에셋) | 아키타입에 고정되지 않는 모델. 각 모델은 **어울리는 아키타입 멀티라벨**(적합 집합)을 가짐 — 아래 §2·§6 |
| 이동 AI | 직진 / 추적 / 사행 / 견제 (모듈, 재사용) |
| 공격 AI | 충돌 / 조준단발 / 탄막 / 자폭 (모듈, 재사용) |
| 스탯 | 체력·속도·데미지·드랍 오브량 (조합마다 주입) |

### 아키타입 (4종) — range를 자동 결정
전방(코스 안쪽)에서 플레이어 쪽으로 접근하는 구도 전제.
**아키타입을 고르면 교전거리(range)는 자동으로 결정**된다(독립 선택 아님).

| 아키타입 | 성격 | range(자동) |
|---|---|---|
| 탄막형 | 거리 두고 탄 뿌림 | **원거리** |
| 돌진형 | 체력 높고 느림, 몸통으로 밀고 들어옴 | **근거리** |
| 자폭형 | 체력 낮고 빠름, 붙으면 범위 폭발 | **근거리** |
| 복합형 | 돌진하며 원거리도 사격(하이브리드, 고난이도) | **복합** |

> 돌진형 vs 자폭형 차별화: 돌진형은 몸통 단발 충돌(스치면 끝, 쏴서 처리 대상),
> 자폭형은 근접 시 **범위** 폭발(못 스치고 지나감, 미리 요격 대상).

### 비주얼 ↔ AI 분리 & 라벨 규칙 (조합형의 핵심)
- **모델(비주얼)과 AI/아키타입은 분리.** 어떤 모델에든 어떤 AI든 붙일 수 있다. 모델 이름(예: `Enemy_Barrage_2`)은 **사람 식별용 편의**일 뿐, 그 역할만 한다는 뜻이 아니다.
- **모델 라벨 = `archetype:` 멀티라벨**(그 모델이 자연스럽게 어울리는 아키타입 **집합**). 예: `[탄막, 돌진, 복합]` 또는 `[탄막, 돌진]`. `range:` 라벨은 아키타입에서 파생되므로 자동.
- **툴에서 모델 라벨에 없는 아키타입을 고르면 → "부자연스러운 조합" 경고만**(차단 아님, §4). 비주얼 부조화 방지는 오서링하는 사람의 판단.
- 조합·변형의 자유도는 이 라벨층이 아니라 **SO / 아키타입 프로파일층(§5 "반쯤 묶기")** 에서 나온다.

## 3. AI 프리미티브

### 이동 AI
| 타입 | 동작 | 거리 성향 |
|---|---|---|
| 직진 | 코스 따라 곧장 접근 | 접근 |
| 추적 | 플레이어 XY 향해 보정하며 접근 | 접근 |
| 사행 | 좌우 사인파로 흔들며 접근 | 접근 |
| 견제(호버) | 일정 거리에서 머물며 사격 후 이탈 | 거리 유지 |

> **구현 완료(M4-7, 2026-08-21)**: 이동4×공격4 전부 동작. **견제**=접근→선호 Z거리서 정지→머무는 시간 사격→-Z 재접근해 끝라인 despawn. **사격은 정지 중에만**(이동 중 억제). 선호거리·머무는시간은 SO(`EnemyStats.hoverDistance/hoverDuration`)이며 오서링 툴에서 **MoveAI==견제일 때만 편집**. (사행·조준단발은 M3-3 선반영.) 측면추적 없음(v1)·수치 Day5.

### 공격 AI
| 타입 | 동작 | 요구 교전거리 |
|---|---|---|
| 충돌 | 발사 없음, 몸통 접촉 데미지 | 근접 필수 |
| 조준단발 | 플레이어 방향 한 발 | 무관 |
| 탄막 | 방사형/부채꼴 다발 | 원거리 선호 |
| 자폭 | 근접 시 폭발(범위 데미지) | 근접 필수 |

## 4. 유효성 규칙 — 차단 아님, **경고**

조합은 **자유롭게 선택 가능**. 단, "좀 아닌 것 같은" 조합은 툴이 **경고 표시**한다.
(강제 차단 X — 디자이너가 실험할 여지를 남김.)

핵심 규칙: **공격 AI의 요구 교전거리 ↔ 이동 AI의 거리 성향이 모순이면 경고.**

| 조합 예 | 판정 |
|---|---|
| 자폭 + 견제(호버) | ⚠️ 경고 (멀리 떠서 안 터짐) |
| 충돌 + 견제(호버) | ⚠️ 경고 (안 붙어서 충돌 안 함) |
| 탄막 + 추적 | OK (위협적이지만 유효) |
| 조준단발 + 아무 이동 | OK |

추가 경고: **에셋 라벨의 교전거리 ↔ 부여한 AI 성향 모순** (6절 참고).

## 5. 에디터 커스텀 툴 — 3층 구조

| 층 | 에디터가 관리 |
|---|---|
| 1. 유효성 경고 | 교전거리 모순 조합 / 라벨-AI 모순 → 경고 표시 |
| 2. 아키타입 프로파일 | 아키타입별 AI 성향 가중치 (예: 탄막형 = {탄막 70%, 조준단발 20%, 돌진 10%}) — **반쯤 묶기** |
| 3. 스폰 풀 | 난이도 구간별 어떤 프로파일을 어떤 가중치로 출현시킬지 |

> **반쯤 묶기**: 비주얼-AI를 완전 고정하지도(단조), 완전 자유롭게 두지도(비주얼 부조화) 않음.
> 아키타입별 기본 성향 + 확률적 변형을 **에디터 데이터로** 오서링.

## 6. 에셋 관리 (Addressables)

에셋 특성 관리는 **라벨(거친 분류) + ScriptableObject(정밀 데이터)** 병행.
라벨 하나로는 가중치·수치를 못 담기 때문.

| 계층 | 담는 것 | 용도 |
|---|---|---|
| Addressables **Group** "Enemy" | 적 프리팹(비주얼) | 빌드/번들 물리 묶음 |
| **Label** | `archetype:탄막/돌진/자폭/복합`(모델당 **멀티**=적합 집합), `range:원거리/근거리/복합`(아키타입에서 파생) | 라벨 기준 일괄 로드·필터, 거친 특성 + 유효성 판정 |
| **ScriptableObject DB** | AssetReference + AI 성향 가중치 + 스탯 범위 | 에디터 툴이 편집하는 디자인 원천 데이터 |

### 라벨 ↔ SO 교차 검증
라벨이 유효성 경고의 판정 근거가 된다.

- 에셋 라벨이 `range:근거리`인데 SO에서 그 에셋에 탄막(원거리) AI 프로파일을 주면 → ⚠️ 경고.
- 에셋 `archetype:` 멀티라벨 **집합에 없는** 아키타입을 SO에서 그 모델에 주면 → ⚠️ "부자연스러운 조합" 경고(차단 아님).
- 즉 라벨(빠른 로딩 인덱스 + 적합 집합) + SO(정밀 데이터)가 서로를 검증.

## 7. 스탯 필드 (Day5 수치, 테이블 툴 컬럼 후보)

- 체력 / 이동속도 / 데미지 / 드랍 오브량 / **처치 점수값**
- (공격 AI별) 발사 간격 / 탄속 / 탄막 수 / 자폭 반경 등

## 8. 확정 요약

- 아키타입 4종: 탄막형 / 돌진형 / 자폭형 / **복합형**. **아키타입이 range를 자동 결정**(탄막=원거리, 돌진·자폭=근거리, 복합=복합).
- 모델(비주얼)은 아키타입에 고정 안 됨 — `archetype:` **멀티라벨**(적합 집합)만 가짐. 이름은 식별 편의.
- 이동 AI 4종 / 공격 AI 4종 → 조합형
- 유효성: 교전거리 모순 / 라벨-AI 모순 = **경고(차단 아님)**
- 에디터 3층: 유효성 경고 / 아키타입 프로파일(반쯤 묶기) / 스폰 풀
- 에셋: Addressables Group + Label + ScriptableObject 병행, 라벨-SO 교차 검증

## 9. 남은 과제

- 각 스탯 실제 수치 (Day5 튜닝)
- 아키타입별 성향 가중치 초기값
- 난이도 구간(스폰 풀 시간축): 시간 기반 페이즈 골격은 [progression-design.md](progression-design.md) §2에서 확정 → 구간별 프로파일 큐레이션만 남음
- 에디터 툴 실제 구현(UI Toolkit) — 3층 UI 레이아웃
